package ${packageStr};

import ${entityPackage}.${entityClass};
import tk.mybatis.mapper.common.Mapper;

/**
 * ${entityDesc}数据访问层
 *
 * @author ${author}
 * @created ${time}
 * @version 1.0
 * @since 1.8
 */
public interface ${className} extends Mapper<${entityClass}> {

}